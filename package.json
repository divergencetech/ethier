{
  "name": "@divergencetech/ethier",
  "version": "0.3.6",
  "description": "Golang and Solidity code to make Ethereum development ethier",
  "main": "\"\"",
  "scripts": {
    "build": "go build -o build/ethier ethier/*.go",
    "generate": "PATH=\"$(readlink -f build):${PATH}\" go generate ./...",
    "test": "go test ./...",
    "testverbose": "go test ./... -test.v",
    "coverage": "node ethier/coverage/coverage.js; PATH=\"$(readlink -f build):${PATH}\" go generate ./..."
  },
  "repository": {
    "type": "git",
    "url": "github.com/divergencetech/ethier"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.3"
  },
  "directories": {
    "test": "tests"
  },
  "devDependencies": {
    "solidity-coverage": "^0.7.17"
  }
}
